using UnityEngine;
using System.Collections;

/*
public class InterfaceBehaviour : MonoBehaviour {
    public InterfaceManager mManager = null;
    void OnGUI()
    {
        GUI.depth = int.MinValue;
        //GUI.Box(new Rect(0, Screen.height-50, 200, 50), "poo");
        GUI.Box(new Rect(20, 20, 80,30), mManager.mGrade.ToString());
    }
}*/
